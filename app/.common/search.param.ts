export const DEFAULT_PAGE = 1;

export enum SearchParam {
  PAGE = 'page',
  SORT_ORDER = 'sortOrder',
  ORGANIZATION = 'org',
  EVENT = 'event',
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}
